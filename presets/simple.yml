# Simple preset - Hatなしの単純ループ
#
# AIバックエンドが LOOP_COMPLETE を出力するまで反復実行。
# 用途:
# - 単発のスクリプト作成
# - バグ調査
# - 自由度の高い対話型開発

preset: simple

# Hatシステム無効（単純ループ）
hats: {}

# ループ設定（デフォルト値を使用）
# max_iterations: 100
# completion_keyword: LOOP_COMPLETE
