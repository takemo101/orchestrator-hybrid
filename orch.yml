backend: opencode
auto: false
create_pr: false
max_iterations: 100
preset: simple

worktree:
  enabled: true
  base_dir: ".worktrees"
  copy_files:
    - ".env"

session:
  manager: auto
  prefix: orch
  capture_interval: 500
