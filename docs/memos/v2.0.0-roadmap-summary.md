# orchestrator-hybrid v2.0.0 ロードマップ

## 概要

本ドキュメントは以下のメモを統合し、v2.0.0に向けた実装ロードマップをまとめたものです。

- `ralph-features-to-adopt.md` - Ralph Orchestratorからの機能移植
- `run設定のデフォルト化とリファクタリング.md` - CLI改善
- `worktree-container-use-hybrid.md` - 並列実行環境
- `バックエンド出力のリアルタイムストリーミング.md` - ログ改善

---

## アーキテクチャ概要図

```
                                    orch.yml
                                       │
                                       ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                           orchestrator-hybrid v2.0.0                    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐                 │
│  │   CLI層     │    │   Core層    │    │  Adapter層  │                 │
│  │             │    │             │    │             │                 │
│  │ commands/   │───▶│  loop.ts    │───▶│  claude.ts  │                 │
│  │ run.ts      │    │  event.ts   │    │  opencode.ts│                 │
│  │ logs.ts     │    │  hat.ts     │    │  worktree-  │                 │
│  │ init.ts     │    │             │    │  manager.ts │                 │
│  │             │    │  memories/  │    │             │                 │
│  │ config-     │    │  tasks/     │    │  sandbox-   │                 │
│  │ merger.ts   │    │             │    │  factory.ts │                 │
│  └─────────────┘    └─────────────┘    └─────────────┘                 │
│         │                  │                  │                         │
│         ▼                  ▼                  ▼                         │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │                        実行環境                                    │ │
│  │  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐   │ │
│  │  │ Issue #42       │  │ Issue #43       │  │ Issue #44       │   │ │
│  │  │                 │  │                 │  │                 │   │ │
│  │  │ .worktrees/42/  │  │ .worktrees/43/  │  │ .worktrees/44/  │   │ │
│  │  │       +         │  │       +         │  │       +         │   │ │
│  │  │ container-use   │  │ container-use   │  │ container-use   │   │ │
│  │  │ env: abc-123    │  │ env: def-456    │  │ env: ghi-789    │   │ │
│  │  └─────────────────┘  └─────────────────┘  └─────────────────┘   │ │
│  └───────────────────────────────────────────────────────────────────┘ │
│                                                                         │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │                       永続化層                                     │ │
│  │  .agent/memories.md    .agent/tasks.jsonl    .agent/sessions/     │ │
│  │  (共有)                (タスク依存関係)        (セッション記録)    │ │
│  └───────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 機能一覧と優先度

### Phase 1: 基盤改善（優先度: 高）

| ID | 機能 | 概要 | 工数 | 依存 |
|----|------|------|------|------|
| P1-1 | **run設定デフォルト化** | `--auto`, `--create-pr`等を設定ファイルで指定可能に | 2h | - |
| P1-2 | **CLIリファクタリング** | commands/分離、config-merger実装 | 6h | P1-1 |
| P1-3 | **バックエンド出力ストリーミング** | リアルタイムでbackend.logに書き込み | 10h | - |
| P1-4 | **logsコマンド拡張** | `--source backend`でAIエージェント出力を監視 | 2h | P1-3 |

### Phase 2: 並列実行環境（優先度: 高）

| ID | 機能 | 概要 | 工数 | 依存 |
|----|------|------|------|------|
| P2-1 | **WorktreeManager** | worktree作成/削除/一覧 | 6h | - |
| P2-2 | **Worktree + Container-Use統合** | ハイブリッド環境の自動構築 | 4h | P2-1 |
| P2-3 | **環境状態管理** | GitHub Issueラベル/メタデータで追跡 | 3h | P2-2 |
| P2-4 | **自動クリーンアップ** | マージ後にworktree + container-use削除 | 2h | P2-3 |

### Phase 3: 永続化・学習（優先度: 中）

| ID | 機能 | 概要 | 工数 | 依存 |
|----|------|------|------|------|
| P3-1 | **Memories System** | `.agent/memories.md`にセッション間で学習を永続化 | 8h | - |
| P3-2 | **Tasks System** | `.agent/tasks.jsonl`でタスク依存関係管理 | 6h | - |
| P3-3 | **Session Recording** | `--record-session`でJSONL記録 | 4h | P1-3 |

### Phase 4: Hat/バックエンド拡張（優先度: 中）

| ID | 機能 | 概要 | 工数 | 依存 |
|----|------|------|------|------|
| P4-1 | **Per-Hat Model Selection** | Hat毎にopus/sonnet/haiku選択 | 4h | - |
| P4-2 | **Per-Hat Backend** | Hat毎に異なるバックエンド | 6h | P4-1 |
| P4-3 | **Custom Backends** | 任意のCLI AIエージェント統合 | 4h | P4-2 |
| P4-4 | **Glob Pattern Event Matching** | `build.*`, `*.done`等のマッチング | 3h | - |

### Phase 5: UX改善（優先度: 低）

| ID | 機能 | 概要 | 工数 | 依存 |
|----|------|------|------|------|
| P5-1 | **Event Emission CLI** | `orch emit`コマンド | 2h | - |
| P5-2 | **TUI Mode** | リアルタイムターミナルUI | 20h+ | P1-3 |

---

## 設定ファイル統合案（orch.yml v2.0）

```yaml
version: "2.0"

# ============================================
# バックエンド設定
# ============================================
backend:
  type: claude                    # claude | opencode | gemini | custom
  model: sonnet                   # グローバルデフォルトモデル
  
  # カスタムバックエンド用
  # command: "my-agent"
  # args: ["--headless"]
  # prompt_mode: "arg"            # arg | stdin
  # prompt_flag: "-p"

# ============================================
# 実行設定（run設定デフォルト化）
# ============================================
run:
  auto_mode: true                 # --auto のデフォルト
  create_pr: true                 # --create-pr のデフォルト
  draft_pr: false                 # --draft のデフォルト

# ============================================
# ループ設定
# ============================================
loop:
  max_iterations: 100
  completion_promise: "LOOP_COMPLETE"
  idle_timeout_secs: 1800

# ============================================
# Worktree設定（並列実行環境）
# ============================================
worktree:
  enabled: true                   # worktree使用
  base_dir: ".worktrees"          # worktree格納ディレクトリ
  auto_cleanup: true              # マージ後に自動削除
  copy_env_files:                 # コピーする環境ファイル
    - ".env"
    - ".envrc"
    - ".env.local"

# ============================================
# Container-Use設定
# ============================================
container:
  enabled: true                   # container-use使用
  image: node:20

# ============================================
# PR設定
# ============================================
pr:
  auto_merge: true
  merge_method: squash            # squash | merge | rebase
  delete_branch: true
  ci_timeout_secs: 1800

# ============================================
# 依存関係設定
# ============================================
dependency:
  resolve: true                   # 依存Issueを先に実行
  ignore: false

# ============================================
# 状態管理
# ============================================
state:
  use_github_labels: true
  label_prefix: "orch"
  use_scratchpad: true
  scratchpad_path: ".agent/scratchpad.md"

# ============================================
# Memories System（セッション間学習）
# ============================================
memories:
  enabled: true
  inject: auto                    # auto | manual | none
  path: ".agent/memories.md"

# ============================================
# Tasks System（タスク追跡）
# ============================================
tasks:
  enabled: true
  path: ".agent/tasks.jsonl"

# ============================================
# セッション記録
# ============================================
session:
  record: false                   # --record-session のデフォルト
  path: ".agent/sessions/"

# ============================================
# Hat定義（Per-Hat Model/Backend対応）
# ============================================
hats:
  planner:
    name: "📋 Planner"
    triggers: ["task.start"]
    publishes: ["plan.ready"]
    model: opus                   # このHatはOpus使用
    instructions: |
      計画を立てる...

  implementer:
    name: "🔨 Implementer"
    triggers: ["plan.ready"]
    publishes: ["code.written"]
    # model省略 → backend.modelを継承
    instructions: |
      実装する...

  reviewer:
    name: "🔍 Reviewer"
    triggers: ["code.written"]
    publishes: ["review.approved", "LOOP_COMPLETE"]
    model: haiku                  # 軽量モデルで高速レビュー
    backend: gemini               # 別バックエンドも可
    instructions: |
      レビューする...

# ============================================
# 品質設定
# ============================================
quality:
  min_score: 8
  auto_approve_above: 9

# ============================================
# 承認ゲート
# ============================================
gates:
  after_plan: true
  after_implementation: false
  before_pr: true

# ============================================
# 改善Issue自動作成
# ============================================
auto_issue:
  enabled: true
  min_priority: medium
  labels:
    - auto-generated
    - improvement
```

---

## ログディレクトリ構成（v2.0）

```
.agent/
├── memories.md                   # セッション間で共有（シンボリックリンク対応）
├── tasks.jsonl                   # タスク依存関係
├── scratchpad.md                 # セッション内メモ
├── sessions/                     # セッション記録
│   └── session-2026-01-26.jsonl
│
├── task-1234567890-42/           # Issue #42 のタスク
│   ├── task.log                  # orchestrator-hybridログ
│   ├── backend.log               # バックエンド出力（リアルタイム）
│   ├── events.jsonl              # イベント履歴
│   └── report.md                 # 実行レポート
│
└── task-1234567890-43/           # Issue #43 のタスク
    └── ...

.worktrees/
├── issue-42/                     # Issue #42 のworktree
│   ├── .agent/
│   │   ├── memories.md → ../../.agent/memories.md  # シンボリックリンク
│   │   ├── scratchpad.md         # 独立
│   │   └── ...
│   └── [プロジェクトファイル]
│
└── issue-43/                     # Issue #43 のworktree
    └── ...
```

---

## 実装スケジュール案

### Week 1-2: Phase 1（基盤改善）
- [ ] P1-1: run設定デフォルト化
- [ ] P1-2: CLIリファクタリング
- [ ] P1-3: バックエンド出力ストリーミング
- [ ] P1-4: logsコマンド拡張

### Week 3-4: Phase 2（並列実行環境）
- [ ] P2-1: WorktreeManager
- [ ] P2-2: Worktree + Container-Use統合
- [ ] P2-3: 環境状態管理
- [ ] P2-4: 自動クリーンアップ

### Week 5-6: Phase 3（永続化・学習）
- [ ] P3-1: Memories System
- [ ] P3-2: Tasks System
- [ ] P3-3: Session Recording

### Week 7-8: Phase 4（Hat/バックエンド拡張）
- [ ] P4-1: Per-Hat Model Selection
- [ ] P4-2: Per-Hat Backend
- [ ] P4-3: Custom Backends
- [ ] P4-4: Glob Pattern Event Matching

### Week 9+: Phase 5（UX改善）
- [ ] P5-1: Event Emission CLI
- [ ] P5-2: TUI Mode（優先度低、工数大）

---

## 工数サマリー

| Phase | 工数合計 | 主要成果物 |
|-------|---------|-----------|
| Phase 1 | 約20時間 | CLIリファクタリング、リアルタイムログ |
| Phase 2 | 約15時間 | Worktree + Container-Useハイブリッド |
| Phase 3 | 約18時間 | Memories、Tasks、Session Recording |
| Phase 4 | 約17時間 | Per-Hat Model/Backend、Glob Matching |
| Phase 5 | 約22時間+ | Event CLI、TUI |
| **合計** | **約92時間+** | |

---

## 関連ドキュメント

| ドキュメント | 用途 |
|-------------|------|
| `docs/requirements/REQ-ORCH-001_追加仕様.md` | 要件定義書 |
| `docs/memos/ralph-features-to-adopt.md` | Ralph機能詳細 |
| `docs/memos/run設定のデフォルト化とリファクタリング.md` | CLI改善詳細 |
| `docs/memos/worktree-container-use-hybrid.md` | 並列環境詳細 |
| `docs/memos/バックエンド出力のリアルタイムストリーミング.md` | ログ改善詳細 |
| `.opencode/skill/create-worktree/` | worktreeスクリプト参考 |
| `.opencode/skill/container-use-guide/` | container-useガイド |

---

## 備考

- v2.0.0は大規模リリースのため、段階的にv1.4.0, v1.5.0等でリリースする選択肢もあり
- TUI Mode（P5-2）は工数が大きいため、v2.1.0以降に延期も検討
- 既存テスト（426テスト）との互換性維持が必要
