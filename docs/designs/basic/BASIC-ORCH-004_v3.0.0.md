# orchestrator-hybrid v3.0.0 基本設計書

## メタ情報

| 項目 | 内容 |
|------|------|
| ドキュメントID | BASIC-ORCH-004 |
| バージョン | 1.0.0 |
| ステータス | ドラフト |
| 作成日 | 2026-01-28 |
| 対応要件定義書 | REQ-ORCH-004 v3.0.0 |
| 作成者 | AI Assistant |

---

## 1. 概要

### 1.1 目的

orchestrator-hybrid v3.0.0は、**シンプル化**と**実用性**を最優先とした再設計版である。
v2.0.0までに肥大化した機能を大幅に削減し、本当に必要な機能のみに集中する。

**設計目標**:
- ソースファイル数: 100+ → **20以下**
- ドキュメント数: 91 → **10以下**
- 設定オプション数: 50+ → **10以下**

### 1.2 背景

v1.2.0からv2.0.0にかけて、以下の問題が発生した:

| 問題 | 詳細 |
|------|------|
| **機能の肥大化** | 使用頻度の低い機能が多数追加された |
| **複雑な設定** | 50以上の設定オプションが存在 |
| **保守性の低下** | 100以上のソースファイル、91のドキュメント |
| **学習コストの増加** | 新規ユーザーが理解するのに時間がかかる |

### 1.3 設計方針

| 方針 | 説明 |
|------|------|
| **コアへの回帰** | Issue → AI → PR の基本フローに集中 |
| **削除 > 追加** | 使われない機能は削除 |
| **シンプルな設定** | 5つ以下のオプションで動作 |
| **実用的な機能** | Worktree環境分離、セッション管理を追加 |

### 1.4 スコープ

#### スコープ内

- コア機能（F-001〜F-005）の維持
- Hatシステムの簡略化（F-006）
- プリセット機能（F-007）
- ログ監視（F-008）
- ステータスラベル（F-009）
- Issue依存関係（F-010）
- **新規**: Worktree環境分離（F-011）
- **新規**: セッション管理（F-012）

#### スコープ外

- Memories System（AIコンテキストで代替）
- Tasks System（GitHub Issueで代替）
- Session Recording（使用頻度低）
- Multi-Loop Concurrency（Worktreeで代替）
- Custom Backends（使用ケース限定的）
- Event Emission CLI（内部処理で十分）
- Glob Pattern Matching（完全一致で十分）
- Per-Hat Model/Backend（1モデルで十分）
- Container-use統合（Worktreeで代替）
- 環境状態管理（過剰な複雑性）
- 自動クリーンアップ（手動で十分）

### 1.5 用語定義

| 用語 | 定義 |
|------|------|
| **Hat** | 役割ベースのプロンプト切り替え機構（例: Tester, Implementer） |
| **Preset** | 事前定義された設定セット（simple, tdd） |
| **Worktree** | git worktreeを使用した並列実行環境 |
| **Session Manager** | バックエンド実行を管理する抽象化レイヤー |
| **Backend** | AIエージェント（claude, opencode） |
| **Loop** | `LOOP_COMPLETE`が出力されるまでの反復実行 |
| **Approval Gate** | 人間の承認を要求するチェックポイント |

---

## 2. システムアーキテクチャ

### 2.1 全体構成

```
┌─────────────────────────────────────────────────────────────┐
│                         CLI Layer                            │
│  (orch run, orch logs, orch sessions, orch worktrees)       │
└──────────────────────┬──────────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────────┐
│                      Core Layer                              │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │ Loop Engine  │  │ Hat System   │  │ Event Bus    │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │ Config Mgr   │  │ Worktree Mgr │  │ Session Mgr  │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
└──────────────────────┬──────────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────────┐
│                    Adapters Layer                            │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │ Claude       │  │ OpenCode     │  │ Session Impl │     │
│  │ Adapter      │  │ Adapter      │  │ (3 types)    │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
└──────────────────────┬──────────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────────┐
│                   External Systems                           │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │ GitHub API   │  │ git worktree │  │ tmux/zellij  │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 レイヤー構成

| レイヤー | 責務 | 主要コンポーネント |
|---------|------|------------------|
| **CLI** | ユーザーインターフェース | commander, chalk |
| **Core** | ビジネスロジック | Loop, Hat, Event, Config, Worktree, Session |
| **Adapters** | 外部システム統合 | Claude, OpenCode, SessionManager実装 |
| **External** | 外部依存 | GitHub API, git, tmux/zellij |

### 2.3 技術スタック

| レイヤー | 技術 | バージョン | 選定理由 |
|---------|------|-----------|---------|
| **Runtime** | Bun | 1.0+ | 高速、TypeScript-native、ゼロコンフィグ |
| **Language** | TypeScript | 5.0+ | 型安全性、開発効率 |
| **CLI** | commander | 12.0+ | 標準的なCLIライブラリ、豊富な機能 |
| **Schema** | zod | 3.0+ | ランタイム型検証、TypeScript統合 |
| **Output** | chalk | 5.0+ | 色付きターミナル出力 |
| **Config** | yaml | 2.0+ | 人間が読みやすい設定形式 |
| **Test** | Bun test | - | ネイティブ、ゼロコンフィグ |
| **Linter** | Biome | 1.0+ | 高速、統一されたツール |

#### 削除予定パッケージ

| パッケージ | 削除理由 |
|-----------|---------|
| @inquirer/prompts | 承認ゲートをシンプル化可能 |
| proper-lockfile | Multi-Loop削除により不要 |

### 2.4 前提条件

| ツール | バージョン | 必須 | 用途 |
|--------|-----------|------|------|
| Bun | 1.0+ | ✅ | ランタイム |
| git | 2.5+ | ✅ | worktree機能 |
| gh | 最新 | ✅ | GitHub CLI |
| claude/opencode | 最新 | ✅ | AIバックエンド |
| tmux | 3.0+ | ❌ | セッション管理（オプション） |
| zellij | 最新 | ❌ | セッション管理（オプション） |

### 2.5 外部システム連携

```
orchestrator-hybrid
        │
        ├─▶ GitHub API
        │     ├─ Issue取得
        │     ├─ PR作成
        │     └─ ラベル管理
        │
        ├─▶ git worktree
        │     ├─ 環境作成
        │     ├─ ブランチ管理
        │     └─ 環境削除
        │
        └─▶ AI Backend (claude/opencode)
              ├─ プロンプト送信
              ├─ 出力受信
              └─ セッション管理
```

---

## 3. 機能一覧

### 3.1 機能概要

| 機能ID | 機能名 | カテゴリ | 優先度 | フェーズ |
|--------|--------|---------|--------|---------|
| F-001 | Issue取得 | コア | 高 | Phase 1 |
| F-002 | プロンプト生成 | コア | 高 | Phase 1 |
| F-003 | ループ実行 | コア | 高 | Phase 1 |
| F-004 | PR作成 | コア | 高 | Phase 1 |
| F-005 | 承認ゲート | コア | 中 | Phase 1 |
| F-006 | Hatシステム | 拡張 | 中 | Phase 2 |
| F-007 | プリセット | 拡張 | 中 | Phase 2 |
| F-008 | ログ監視 | 運用 | 中 | Phase 3 |
| F-009 | ステータスラベル | 運用 | 低 | Phase 3 |
| F-010 | Issue依存関係 | 拡張 | 低 | Phase 4 |
| F-011 | Worktree環境分離 | 新規 | 高 | Phase 3 |
| F-012 | セッション管理 | 新規 | 高 | Phase 3 |

### 3.2 機能詳細

#### F-001: Issue取得

**概要**: GitHub Issueからタスク内容を取得

**処理フロー**:
```
orch run --issue 42
    │
    ├─▶ gh CLI で Issue #42 を取得
    │
    ├─▶ タイトル、本文、ラベルを抽出
    │
    └─▶ プロンプト生成に渡す
```

**入力**: Issue番号
**出力**: Issue情報（タイトル、本文、ラベル）

---

#### F-002: プロンプト生成

**概要**: Issue内容からAIに渡すプロンプトを自動生成

**処理フロー**:
```
Issue情報
    │
    ├─▶ テンプレート読み込み
    │
    ├─▶ Issue内容を埋め込み
    │
    ├─▶ Hat指示を追加（Hat有効時）
    │
    └─▶ .agent/PROMPT.md に保存
```

**入力**: Issue情報、Hat設定
**出力**: `.agent/PROMPT.md`

---

#### F-003: ループ実行

**概要**: `LOOP_COMPLETE`が出力されるまで反復実行

**処理フロー**:
```
開始
  │
  ├─▶ イテレーション開始
  │     │
  │     ├─▶ AIバックエンド実行
  │     │
  │     ├─▶ 出力に "LOOP_COMPLETE" があるか確認
  │     │     │
  │     │     ├─ なし → 次のイテレーション
  │     │     │
  │     │     └─ あり → ループ終了
  │     │
  │     └─▶ 最大反復回数チェック
  │
  └─▶ 完了
```

**入力**: プロンプト、最大反復回数
**出力**: 完了フラグ

---

#### F-004: PR作成

**概要**: 完了後にPRを自動作成

**処理フロー**:
```
ループ完了
    │
    ├─▶ 変更をコミット
    │
    ├─▶ gh pr create でPR作成
    │
    └─▶ PR URLを出力
```

**入力**: ブランチ名、Issue番号
**出力**: PR URL

---

#### F-005: 承認ゲート

**概要**: 重要ポイントで人間の承認を要求

**処理フロー**:
```
承認ゲート到達
    │
    ├─▶ 現在の状態を表示
    │
    ├─▶ ユーザーに承認を要求
    │     │
    │     ├─ 承認 → 続行
    │     │
    │     └─ 拒否 → 中断
    │
    └─▶ 次の処理へ
```

**入力**: ゲート種別（pre-loop, post-completion, before-pr）
**出力**: 承認/拒否

---

#### F-006: Hatシステム（簡略化版）

**概要**: 役割ベースのプロンプト切り替え

**簡略化内容**:
- Per-Hat Model削除（全Hatで同一モデル）
- Per-Hat Backend削除（全Hatで同一バックエンド）
- イベントバスは維持

**処理フロー**:
```
イベント発行
    │
    ├─▶ トリガーに一致するHatを検索
    │
    ├─▶ Hat指示をプロンプトに追加
    │
    ├─▶ AIバックエンド実行
    │
    └─▶ 出力から次のイベントを抽出
```

---

#### F-007: プリセット

**概要**: 定義済み設定（simple/tdd）

**プリセット一覧**:

| プリセット | 説明 | Hat構成 |
|-----------|------|---------|
| **simple** | Hatなしの単純ループ | なし |
| **tdd** | Red → Green → Refactor | Tester, Implementer, Refactorer |

---

#### F-008: ログ監視

**概要**: `orch logs`コマンドでリアルタイムログ表示

**処理フロー**:
```
orch logs --follow
    │
    ├─▶ SessionManager.streamOutput() を呼び出し
    │
    ├─▶ 出力をリアルタイムで表示
    │
    └─▶ Ctrl+C で終了
```

**入力**: Issue番号（オプション）
**出力**: リアルタイムログストリーム

---

#### F-009: ステータスラベル

**概要**: GitHub Issueにステータスラベルを自動付与

**ステータス一覧**:

| ステータス | ラベル | 色 |
|-----------|--------|-----|
| queued | `orch:queued` | 🟢 薄緑 |
| running | `orch:running` | 🟢 緑 |
| completed | `orch:completed` | 🔵 青 |
| failed | `orch:failed` | 🔴 赤 |
| blocked | `orch:blocked` | 🟡 黄 |
| pr-created | `orch:pr-created` | 🟣 紫 |
| merged | `orch:merged` | 🔵 濃青 |

---

#### F-010: Issue依存関係

**概要**: 依存Issueを先に実行

**処理フロー**:
```
orch run --issue 42 --resolve-deps
    │
    ├─▶ Issue #42 の依存関係を取得
    │
    ├─▶ 依存Issueを再帰的に実行
    │
    └─▶ Issue #42 を実行
```

---

#### F-011: Worktree環境分離（新規）

**概要**: git worktreeで並列実行環境を完全分離

**処理フロー**:
```
orch run --issue 42
    │
    ├─[1] worktree作成
    │     git worktree add -b feature/issue-42 .worktrees/issue-42 main
    │
    ├─[2] 環境ファイルコピー
    │     cp .env .envrc .worktrees/issue-42/
    │
    ├─[3] worktree内で実行
    │     cd .worktrees/issue-42 && orch run --in-worktree
    │
    ├─[4] PR作成
    │     gh pr create --head feature/issue-42
    │
    └─[5] マージ後に手動クリーンアップ
          orch worktree remove 42
```

**ディレクトリ構造**:
```
.worktrees/
├── issue-42/
│   ├── src/
│   ├── .env
│   └── .envrc
└── issue-43/
    ├── src/
    └── .env
```

**ビジネスルール**:
- BR-011-1: worktreeは`.worktrees/issue-<番号>/`に作成
- BR-011-2: ブランチ名は`feature/issue-<番号>`
- BR-011-3: 環境ファイルは自動コピー
- BR-011-4: クリーンアップは手動（`orch worktree remove`）
- BR-011-5: 実行中のIssueのworktreeは削除不可（`orch:running`ラベルをチェック）

---

#### F-012: セッション管理（新規）

**概要**: バックエンド実行をセッションとして管理

**アーキテクチャ**:
```
ISessionManager (Interface)
    │
    ├─▶ NativeSessionManager (依存なし)
    │     └─ Bun.spawn + ファイルログ
    │
    ├─▶ TmuxSessionManager (tmux)
    │     └─ tmux commands
    │
    └─▶ ZellijSessionManager (zellij)
          └─ zellij actions
```

**インターフェース**:
```typescript
interface ISessionManager {
  create(id: string, command: string, args: string[]): Promise<Session>;
  list(): Promise<Session[]>;
  getOutput(id: string, lines?: number): Promise<string>;
  streamOutput(id: string): AsyncIterable<string>;
  attach(id: string): Promise<void>;
  isRunning(id: string): Promise<boolean>;
  kill(id: string): Promise<void>;
}
```

**セッションマネージャー比較**:

| 実装 | 依存 | attach機能 | 対話性 | 推奨用途 |
|------|------|-----------|--------|---------|
| **native** | なし | tail -f | 読み取りのみ | CI/CD、サーバー |
| **tmux** | tmux | 完全対話 | 完全 | ローカル開発 |
| **zellij** | zellij | 完全対話 | 完全 | モダン環境 |

**自動検出ロジック**:
```
1. 設定で明示的に指定されている場合はそれを使用
2. tmuxがインストールされている場合はTmuxSessionManager
3. zellijがインストールされている場合はZellijSessionManager
4. フォールバック: NativeSessionManager（依存なし）
```

**ビジネスルール**:
- BR-012-1: デフォルトは自動検出（tmux > zellij > native）
- BR-012-2: セッション名は`orch-<issue番号>`
- BR-012-3: 出力は500ms間隔でキャプチャ
- BR-012-4: `LOOP_COMPLETE`検出でセッション終了
- BR-012-5: エラー時はセッションを保持（デバッグ用）
- BR-012-6: native モードでも基本機能は動作保証

---

## 4. 画面一覧（CLIコマンド）

### 4.1 コマンド一覧

| 画面ID | コマンド | 説明 | 優先度 |
|--------|---------|------|--------|
| CMD-001 | `orch run <issue>` | Issue実行 | 高 |
| CMD-002 | `orch status` | 実行状態確認 | 中 |
| CMD-003 | `orch logs` | ログ確認 | 中 |
| CMD-004 | `orch sessions` | セッション一覧 | 中 |
| CMD-005 | `orch attach <issue>` | セッションにアタッチ | 中 |
| CMD-006 | `orch kill <issue>` | セッション終了 | 低 |
| CMD-007 | `orch worktrees` | worktree一覧 | 低 |
| CMD-008 | `orch worktree remove <issue>` | worktree削除 | 低 |
| CMD-009 | `orch init` | 設定初期化 | 低 |

### 4.2 CMD-001: orch run

**概要**: Issue実行

**構文**:
```bash
orch run --issue <番号> [オプション]
```

**オプション**:

| オプション | 短縮形 | 説明 | デフォルト |
|-----------|--------|------|-----------|
| `--issue <番号>` | `-i` | **必須** Issue番号 | - |
| `--auto` | `-a` | 承認ゲート自動化 | false |
| `--create-pr` | | PR自動作成 | false |
| `--preset <名前>` | `-p` | プリセット使用 | simple |
| `--backend <種類>` | `-b` | バックエンド指定 | claude |
| `--max-iterations <数>` | `-m` | 最大反復回数 | 100 |
| `--resolve-deps` | | 依存Issue先行実行 | false |
| `--no-worktree` | | worktree無効化 | false |
| `--session-manager <type>` | | セッションマネージャー指定 | auto |

**実行例**:
```bash
# 基本実行
orch run --issue 42

# 完全自動実行
orch run --issue 42 --auto --create-pr

# TDDプリセット
orch run --issue 42 --preset tdd --auto

# tmuxセッションで実行
orch run --issue 42 --session-manager tmux
```

---

### 4.3 CMD-003: orch logs

**概要**: ログ確認

**構文**:
```bash
orch logs [issue番号] [オプション]
```

**オプション**:

| オプション | 短縮形 | 説明 | デフォルト |
|-----------|--------|------|-----------|
| `--follow` | `-f` | リアルタイムストリーミング | false |

**実行例**:
```bash
# 最新のセッションログを表示
orch logs

# 特定Issueのログを表示
orch logs 42

# リアルタイムストリーミング
orch logs --follow
orch logs 42 --follow
```

---

### 4.4 CMD-004: orch sessions

**概要**: セッション一覧

**構文**:
```bash
orch sessions
```

**出力例**:
```
ID      Status    Started             Duration
42      running   2026-01-28 10:30    5m 30s
43      completed 2026-01-28 09:00    12m 45s
```

---

### 4.5 CMD-005: orch attach

**概要**: セッションにアタッチ

**構文**:
```bash
orch attach <issue番号>
```

**動作**:
- **tmux/zellij**: 完全対話モード
- **native**: `tail -f`でログ表示

**実行例**:
```bash
orch attach 42
```

---

### 4.6 CMD-007: orch worktrees

**概要**: worktree一覧

**構文**:
```bash
orch worktrees
```

**出力例**:
```
Issue   Branch              Path                    Status
42      feature/issue-42    .worktrees/issue-42     running
43      feature/issue-43    .worktrees/issue-43     completed
```

---

### 4.7 CMD-008: orch worktree remove

**概要**: worktree削除

**構文**:
```bash
orch worktree remove <issue番号>
```

**動作**:
1. `orch:running`ラベルをチェック（BR-011-5）
2. 実行中の場合はエラー
3. worktreeを削除

**実行例**:
```bash
orch worktree remove 42
```

---

## 5. API設計（内部インターフェース）

### 5.1 ISessionManager

**概要**: セッション管理の抽象インターフェース

**定義**:
```typescript
interface ISessionManager {
  /**
   * セッションを作成して開始
   */
  create(id: string, command: string, args: string[]): Promise<Session>;
  
  /**
   * セッション一覧を取得
   */
  list(): Promise<Session[]>;
  
  /**
   * 出力を取得（最新N行）
   */
  getOutput(id: string, lines?: number): Promise<string>;
  
  /**
   * 出力をリアルタイムでストリーム
   */
  streamOutput(id: string): AsyncIterable<string>;
  
  /**
   * セッションにアタッチ（対話モード）
   */
  attach(id: string): Promise<void>;
  
  /**
   * セッションが実行中か確認
   */
  isRunning(id: string): Promise<boolean>;
  
  /**
   * セッションを終了
   */
  kill(id: string): Promise<void>;
}

interface Session {
  id: string;
  command: string;
  args: string[];
  startedAt: Date;
  status: 'running' | 'completed' | 'failed';
  exitCode?: number;
  logPath?: string;  // native モードのみ
}
```

**実装クラス**:

| クラス | ファイル | 依存 |
|--------|---------|------|
| NativeSessionManager | `src/core/session/native.ts` | なし |
| TmuxSessionManager | `src/core/session/tmux.ts` | tmux |
| ZellijSessionManager | `src/core/session/zellij.ts` | zellij |

---

### 5.2 IWorktreeManager

**概要**: Worktree管理の抽象インターフェース

**定義**:
```typescript
interface IWorktreeManager {
  /**
   * worktreeを作成
   */
  create(issueNumber: number): Promise<WorktreeInfo>;
  
  /**
   * worktree一覧を取得
   */
  list(): Promise<WorktreeInfo[]>;
  
  /**
   * worktreeを削除
   */
  remove(issueNumber: number): Promise<void>;
  
  /**
   * worktreeが存在するか確認
   */
  exists(issueNumber: number): Promise<boolean>;
}

interface WorktreeInfo {
  issueNumber: number;
  branch: string;
  path: string;
  status: 'running' | 'completed';
}
```

---

### 5.3 IBackend

**概要**: AIバックエンドの抽象インターフェース

**定義**:
```typescript
interface IBackend {
  /**
   * プロンプトを実行
   */
  execute(prompt: string): Promise<BackendResult>;
  
  /**
   * バックエンド名を取得
   */
  getName(): string;
}

interface BackendResult {
  output: string;
  exitCode: number;
  durationMs: number;
}
```

**実装クラス**:

| クラス | ファイル | コマンド |
|--------|---------|---------|
| ClaudeBackend | `src/adapters/claude.ts` | `claude` |
| OpenCodeBackend | `src/adapters/opencode.ts` | `opencode` |

---

## 6. データモデル

### 6.1 設定ファイル構造

**ファイル**: `orch.yml`

**スキーマ**:
```yaml
# バックエンド設定
backend: claude              # claude | opencode

# 自動化設定
auto: true                   # 承認ゲートを自動化
create_pr: true              # 完了後にPR作成
max_iterations: 100          # 最大反復回数

# プリセット（オプション）
preset: tdd                  # simple | tdd

# Worktree環境分離（オプション）
worktree:
  enabled: true
  base_dir: ".worktrees"
  copy_files:
    - ".env"
    - ".envrc"

# セッション管理（オプション）
session:
  manager: auto              # auto | native | tmux | zellij
  prefix: "orch"
  capture_interval: 500

# Issue依存関係（オプション）
dependency:
  resolve: true

# ステータスラベル（オプション）
labels:
  enabled: true
  prefix: "orch"
```

**TypeScript型定義**:
```typescript
interface Config {
  backend: 'claude' | 'opencode';
  auto?: boolean;
  create_pr?: boolean;
  max_iterations?: number;
  preset?: 'simple' | 'tdd';
  worktree?: WorktreeConfig;
  session?: SessionConfig;
  dependency?: DependencyConfig;
  labels?: LabelsConfig;
}

interface WorktreeConfig {
  enabled: boolean;
  base_dir: string;
  copy_files: string[];
}

interface SessionConfig {
  manager: 'auto' | 'native' | 'tmux' | 'zellij';
  prefix: string;
  capture_interval: number;
}
```

---

### 6.2 セッションデータ構造

**ディレクトリ**: `.agent/sessions/`

**native モード**:
```
.agent/sessions/
├── issue-42/
│   ├── stdout.log      # 標準出力
│   ├── stderr.log      # 標準エラー  
│   ├── combined.log    # 両方統合
│   ├── meta.json       # セッション情報
│   └── pid             # プロセスID
```

**meta.json**:
```json
{
  "id": "42",
  "command": "claude",
  "args": ["--prompt", "..."],
  "startedAt": "2026-01-28T10:30:00Z",
  "status": "running",
  "exitCode": null
}
```

---

### 6.3 Worktreeデータ構造

**ディレクトリ**: `.worktrees/`

**構造**:
```
.worktrees/
├── issue-42/
│   ├── src/
│   ├── .env
│   ├── .envrc
│   └── .agent/
│       └── PROMPT.md
└── issue-43/
    ├── src/
    └── .env
```

---

## 7. 非機能要件

### 7.1 パフォーマンス

| 要件 | 目標値 | 測定方法 |
|------|--------|---------|
| 起動時間 | 100ms以下 | `time orch --version` |
| セッション出力キャプチャ遅延 | 500ms以下 | 設定値 |
| worktree作成時間 | 3秒以下 | `time git worktree add` |

### 7.2 可用性

| 要件 | 目標値 |
|------|--------|
| セッション復帰 | プロセス中断後も復帰可能 |
| エラーハンドリング | 全エラーをキャッチして適切なメッセージを表示 |

### 7.3 拡張性

| 要件 | 実現方法 |
|------|---------|
| 新規バックエンド追加 | IBackendインターフェースを実装 |
| 新規セッションマネージャー追加 | ISessionManagerインターフェースを実装 |
| 新規プリセット追加 | `presets/`ディレクトリにYAMLファイルを追加 |

---

## 8. セキュリティ方針

### 8.1 認証・認可

| 項目 | 方針 |
|------|------|
| GitHub認証 | `gh` CLIの認証情報を使用 |
| API Token | 環境変数で管理（`.env`） |

### 8.2 データ保護

| 項目 | 方針 |
|------|------|
| ログファイル | `.agent/`ディレクトリに保存、`.gitignore`に追加 |
| 環境ファイル | worktreeにコピー、`.gitignore`に追加 |

---

## 9. 詳細設計書一覧

| # | 機能名 | パス | ステータス |
|---|--------|------|-----------|
| 1 | Issue取得 | `docs/designs/detailed/v3.0.0/issue-fetcher.md` | 未作成 |
| 2 | プロンプト生成 | `docs/designs/detailed/v3.0.0/prompt-generator.md` | 未作成 |
| 3 | ループ実行 | `docs/designs/detailed/v3.0.0/loop-engine.md` | 未作成 |
| 4 | PR作成 | `docs/designs/detailed/v3.0.0/pr-creator.md` | 未作成 |
| 5 | Hatシステム | `docs/designs/detailed/v3.0.0/hat-system.md` | 未作成 |
| 6 | Worktree管理 | `docs/designs/detailed/v3.0.0/worktree-manager.md` | 未作成 |
| 7 | セッション管理 | `docs/designs/detailed/v3.0.0/session-manager.md` | 未作成 |
| 8 | ログ監視 | `docs/designs/detailed/v3.0.0/log-monitor.md` | 未作成 |

---

## 10. 制約事項・前提条件

### 10.1 技術的制約

| 制約 | 詳細 |
|------|------|
| Bunランタイム | Bun 1.0以上が必須 |
| git worktree | git 2.5以上が必須 |
| GitHub CLI | `gh`コマンドが必須 |
| AIバックエンド | `claude`または`opencode`が必須 |

### 10.2 ビジネス上の制約

| 制約 | 詳細 |
|------|------|
| ソースファイル数 | 20以下 |
| ドキュメント数 | 10以下 |
| 設定オプション数 | 10以下 |

### 10.3 前提条件

| 前提 | 詳細 |
|------|------|
| GitHub認証 | `gh auth login`済み |
| リポジトリ | Gitリポジトリ内で実行 |
| ブランチ | mainブランチが存在 |

---

## 11. リスクと対策

| リスク | 影響度 | 対策 |
|--------|--------|------|
| tmux/zellij未インストール | 中 | nativeモードにフォールバック |
| worktree作成失敗 | 高 | エラーメッセージを表示、リトライ機能 |
| セッション復帰失敗 | 中 | セッション情報をファイルに保存 |
| 依存Issue循環参照 | 低 | 循環検出ロジックを実装 |
| ディスク容量不足 | 中 | worktree削除コマンドを提供 |

---

## 12. 移行計画

### 12.1 Phase 1: 削除（1週間）

**目標**: 不要な機能を削除

**タスク**:
- [ ] Memories System削除
- [ ] Tasks System削除
- [ ] Session Recording削除
- [ ] Custom Backends削除
- [ ] Event Emission CLI削除
- [ ] Glob Pattern Matching削除
- [ ] Per-Hat Model/Backend削除
- [ ] Container-use統合削除
- [ ] 環境状態管理削除
- [ ] 自動クリーンアップ削除

**成果物**:
- ソースファイル数: 100+ → 50
- ドキュメント数: 91 → 20

---

### 12.2 Phase 2: リファクタリング（1週間）

**目標**: コードを簡略化

**タスク**:
- [ ] 型定義の簡略化（`src/core/types.ts`）
- [ ] 設定スキーマの簡略化（`src/core/config.ts`）
- [ ] CLIコマンドの整理（`src/cli.ts`）
- [ ] テストの整理

**成果物**:
- ソースファイル数: 50 → 30
- 設定オプション数: 50+ → 15

---

### 12.3 Phase 3: 新機能実装（2週間）

**目標**: Worktree環境分離、セッション管理を実装

**タスク**:
- [ ] ISessionManagerインターフェース定義
- [ ] NativeSessionManager実装
- [ ] TmuxSessionManager実装
- [ ] ZellijSessionManager実装
- [ ] IWorktreeManagerインターフェース定義
- [ ] WorktreeManager実装
- [ ] logsコマンド実装
- [ ] sessionsコマンド実装
- [ ] attachコマンド実装
- [ ] worktreesコマンド実装

**成果物**:
- ソースファイル数: 30 → 20
- 新規機能: F-011, F-012

---

### 12.4 Phase 4: ドキュメント整理（1週間）

**目標**: ドキュメントを整理

**タスク**:
- [ ] 不要ドキュメントの削除
- [ ] README更新
- [ ] CONFIGURATION.md作成
- [ ] PRESETS.md作成
- [ ] WORKTREE.md作成
- [ ] SESSION.md作成
- [ ] MIGRATION-v3.md作成

**成果物**:
- ドキュメント数: 20 → 10

---

## 13. 期待される効果

| 指標 | v2.0.0 | v3.0.0 | 削減率 |
|------|--------|--------|-------|
| ソースファイル数 | 100+ | 20 | **80%** |
| ドキュメント数 | 91 | 10 | **89%** |
| 設定オプション数 | 50+ | 10 | **80%** |
| 依存パッケージ数 | 10 | 5 | **50%** |
| テストファイル数 | 40+ | 15 | **62%** |
| 学習時間 | 4時間 | 1時間 | **75%** |

---

## 14. 詳細設計リンク

詳細設計書は以下のディレクトリに格納されます：

- [詳細設計インデックス](../detailed/v3.0.0/README.md)

---

## 15. 変更履歴

| バージョン | 日付 | 変更内容 | 作成者 |
|-----------|------|----------|--------|
| 1.0.0 | 2026-01-28 | 初版作成 | AI Assistant |
| 1.0.1 | 2026-01-28 | 詳細設計リンク追加 | AI Assistant |
